import type { OverviewPageConfig } from '../../../../specification/v2';
import type { ImportOverviewPageV2Parameters } from '../../../..';
/**
 * Creates the configuration (file content) for an overview page
 * @param {object} manifest  - manifest of the app
 * @param {object} jsonSchema - app specific JSON schema
 *
 * @returns {OverviewPageConfig} - the OVP configuration (JSON)
 */
export declare function createOverviewPageConfig(importParameters: ImportOverviewPageV2Parameters): OverviewPageConfig;
